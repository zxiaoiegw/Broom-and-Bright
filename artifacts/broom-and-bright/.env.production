# Loaded automatically by `vite build`. Change this to the real deployed
# API origin before shipping. An env var of the same name set in the host
# build environment (Replit Secrets, etc.) overrides this file.
VITE_API_URL=https://api.truecleankc.com
