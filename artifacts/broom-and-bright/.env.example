# Copy to .env.local for local overrides, or set these in your host's
# build environment for production deploys.

# Origin of the backend API server (no trailing slash).
# Local dev defaults to http://localhost:4000 if unset.
VITE_API_URL=https://api.truecleankc.com
